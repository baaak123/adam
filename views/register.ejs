<!DOCTYPE html>
<html lang="en">

<head>
    <title>ADAM Travel</title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <meta name="description" content="">
    <meta name="keywords" content="">
    <meta name="team" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/vegas.min.css">
    <link rel="stylesheet" href="css/font-awesome.min.css">

    <!-- MAIN CSS -->
    <link rel="stylesheet" href="css/templatemo-style.css">
</head>

<body>
    <!-- 매뉴바 -->
    <%- include('sidebar.ejs') %>
    <% if (user == 'guest') { %>
    <% } else { %>
    <p> user가 존재합니다. </p>
    <% } %>
    <!-- 게시판 내용 추가 -->
    <section id="board">
        <div class="container">
            <div class="row">
                <div class="col-md-12 col-sm-12">
                    <div class="board-content">
                        <h1> </h1>
                        <h3> </h3>

                        <!-- 여기에 게시판의 내용을 추가할 수 있습니다. -->
                        <div class="register-container">
                            <form action="/register" method="POST" class="form-example">
                                <fieldset>
                                    <legend>회원가입</legend>
                                    <div class="form-group">
                                        <input type="text" name="name" class="form-control form-control-user" id="id"
                                            aria-describedby="emailHelp" placeholder="Enter UserName...">
                                    </div>
                                    <div class="form-group">
                                        <input type="email" name="email" class="form-control form-control-user"
                                            id="email" aria-describedby="emailHelp"
                                            placeholder="Enter Email Address...">
                                    </div>
                                    <div class="form-group">
                                        <input type="text" name="phone" class="form-control form-control-user"
                                            id="phone" aria-describedby="emailHelp"
                                            placeholder="Enter Phone Number...">
                                    </div>
                                    <div class="form-group">
                                        <input type="password" name="password" class="form-control form-control-user"
                                            id="password" placeholder="Password">
                                    </div>
                                    <button id="register" type="submit" value="Subscribe">회원가입</button>
                                    <a href="/login" class="btn btn-outline-primary">뒤로</a>
                                </fieldset>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- SCRIPTS -->
    <script src="js/jquery.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="js/vegas.min.js"></script>
    <script src="js/custom.js"></script>
    <script>
        $('#register').click(() => {
            const id = $('#id').val();
            const pw = $('#pw').val();

            $.ajax({
                url: '/register',
                method: 'POST',
                data: { id: id, pw: pw },
            }).done((result) => {
                console.log(result);
                console.log(result.message);
            })
        })
    </script>
    <style>
        /* CSS 파일 혹은 <style> 태그 내에 추가 */
        .register-container {
            background-color: #f8f9faa8; /* 배경색 설정 */
            border-radius: 8px; /* 테두리 둥글게 설정 */
            padding: 20px; /* 내부 여백 설정 */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* 그림자 효과 설정 */
            margin-top: 20px; /* 상단 여백 설정 */
            max-width: 400px; /* 추가된 부분 */
            margin: auto; /* 추가된 부분 */
            transform: translate(0%, 80%);
        }

        legend {
            font-size: 1.5em; /* 제목 크기 설정 */
            font-weight: bold; /* 제목 굵게 설정 */
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-control-user {
            border-radius: 5px;
        }

        .custom-control-label {
            font-size: 0.9em; /* 체크박스 레이블 크기 설정 */
        }

        button {
            background-color: #000000; /* 회원가입 버튼 배경색 */
            color: #ffffff; /* 회원가입 버튼 텍스트 색상 */
            padding: 10px 15px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }

        button:hover {
            background-color: #f8f8f8; /* 호버 시 회원가입 버튼 배경색 변경 */
        }

        .btn-outline-primary {
            background-color: #000000; /* 회원가입 버튼 배경색 */
            color: rgb(255, 255, 255); /* 뒤로 버튼 텍스트 색상 */
            border-color: #000000; /* 뒤로 버튼 테두리 색상 */
            padding: 8px 15px;
            border-radius: 10px;
            text-decoration: none;
            display: inline-block;

        }

        .btn-outline-primary:hover {
            background-color: #ffffff; /* 호버 시 뒤로 버튼 배경색 변경 */
            color: #fff; /* 호버 시 뒤로 버튼 텍스트 색상 변경 */
        }
    </style>
</body>

</html>
